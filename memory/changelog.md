# Changelog

## 2026-06-05

- Initialized project memory for pet.

## 2026-06-06

Plan: keep `pet` on existing GitHub remote and harden local Git hygiene.
Changed: switched origin to SSH and updated `.gitignore` to exclude root `.env` and `backend/.env`.
Verified: `ssh -T git@github.com` authenticated as `xiao222xian`; `git remote -v` now uses `git@github.com:xiao222xian/pet-health-app.git`.
Next: classify existing uncommitted app/backend/H5 changes before commit or push.
Cleanup: review nested `petsoul_h5/petsoul-bff/.git`, H5 generated caches, and local material folders.

## 2026-06-17

Plan: move the overseas pet supplement storefront into the long-term `pet` workspace and document current module boundaries without disturbing existing uncommitted app/backend/H5/hardware work.
Changed: added `commerce/README.md`, `commerce/storefront/` static DTC storefront, `PROJECT_STRUCTURE.md`, and updated `AGENT.md` plus memory to include commerce and hardware tracks.
Verified: pending final checks in this session.
Next: classify all uncommitted `pet` changes before commit or push; replace storefront placeholders with approved product facts.
Cleanup: old `experiments/pet-supplement-storefront/` copy can be removed after migration verification.

## 2026-06-25

Plan: migrate the Node `backend/` off Render free tier (kept sleeping after 15min, effectively "down") onto the user's own VPS `103.189.141.67` for persistent uptime. Raw IP + HTTP for now (no domain yet, user confirmed).
Changed: on Ubuntu 22.04 server installed Node 20 + nginx; deployed `backend/` to `/opt/pet-backend` (rsync source + `.env`, `npm ci`, `npm run build`); created systemd unit `pet-backend` (PORT=3000, NODE_ENV=production, Restart=always, enabled on boot); nginx reverse-proxies `:80 → 127.0.0.1:3000`; `ufw` enabled allowing 22/80/443. Created gitignored `app/config.local.sh` pointing `API_BASE_URL` at `http://103.189.141.67/api/v1`. Updated `docs/CHEAP_DEPLOY.md` (self-hosted is now current backend; Render marked legacy).
Verified: external `curl http://103.189.141.67/health` → `{"status":"ok"}`; `POST /api/v1/consult` w/o token → 401 (full nginx→express→auth→Supabase chain live, `.env` loaded); `pet-backend` + `nginx` both `systemctl enabled` (survive reboot).
Next (optional): add domain + Caddy/Let's Encrypt HTTPS so app real-device/H5 can drop cleartext exceptions; consider moving `petsoul_h5/petsoul-bff/` (Python/FastAPI, was on ModelScope Studio) onto same box; harden server (SSH keys, disable root password login, reset the shared root password).
Cleanup: Render `pet-health-backend` service can be deleted; local `backend/dist` (March build) is stale (server built fresh from current source).

## 2026-06-26

Plan: inspect the newly configured `pet.superstar.tots.asia` endpoint without changing business code, compare it with the local backend routing and existing client API configuration, and report the pet service state.
Changed: project memory only; no backend/app source changes.
Verified: `https://pet.superstar.tots.asia/health` returns `{"status":"ok"}`; `https://pet.superstar.tots.asia/` returns Express 404 because `backend/src/index.ts` has no `GET /`; tokenless `POST https://pet.superstar.tots.asia/api/v1/consult` and `/nutrition` return `401 Missing token`, proving nginx reaches Express auth middleware. `http://pet.superstar.tots.asia/` redirects to HTTPS.
Found: bare-IP access no longer hits the pet backend from outside: `http://103.189.141.67/health` and `POST http://103.189.141.67/api/v1/consult` returned nginx 404. `app/config.local.sh` still points to `http://103.189.141.67/api/v1`, so local app runs should be moved to `https://pet.superstar.tots.asia/api/v1`.
Verified after receiving current SSH password: `pet-backend.service` is active/running and enabled, has been up since 2026-06-25 07:32 UTC, and listens on port 3000. Local server `curl http://127.0.0.1:3000/health` returns `{"status":"ok"}`; local root returns Express `Cannot GET /`; tokenless local API POST returns `401 Missing token`. nginx config proxies `pet.superstar.tots.asia` HTTPS to `127.0.0.1:3000`, but HTTP default server returns 404 unless the Host matches `pet.superstar.tots.asia` for HTTPS redirect. Certificate renewal is active via `snap.certbot.renew.timer` (not `certbot.timer`).
Changed: fixed iOS/Flutter backend configuration so App AI calls use `https://pet.superstar.tots.asia/api/v1` instead of the now-broken bare IP or old Railway URL. Updated `app/lib/shared/services/api_service.dart`, `app/run.sh`, `app/config.example.sh`, local `app/config.local.sh`, and removed the obsolete iOS arbitrary HTTP load exception from `app/ios/Runner/Info.plist`.
Verified: no old backend addresses remain in App API config; `app/config.local.sh` expands to `https://pet.superstar.tots.asia/api/v1`; `curl https://pet.superstar.tots.asia/health` returns `{"status":"ok"}`; tokenless consult POST returns `401 Missing token`; `flutter test` passes. `flutter analyze` still exits non-zero due to pre-existing lint/info issues, not this API URL change.
Fixed: resolved Xcode `Module 'app_links' not found` by running `flutter pub get` with the configured Flutter mirror and `pod install` in `app/ios`, regenerating iOS plugin/Pod integration.
Verified: `env PUB_HOSTED_URL=https://pub.flutter-io.cn FLUTTER_STORAGE_BASE_URL=https://storage.flutter-io.cn flutter build ios --simulator --debug` succeeds and builds `build/ios/iphonesimulator/Runner.app`.
Changed: added FLU/OpenAI-compatible AI support to the pet backend and made it the primary AI route for consult and nutrition. Server `/opt/pet-backend/.env` now contains `FLU_BASE_URL=https://new.fluapi.com/v1`, `FLU_MODEL=gpt-5.5`, and a redacted `FLU_API_KEY`; the key was not written into tracked project files.
Verified: local backend `npm run build` and `npm test` pass; deployed backend build succeeds on the VPS; `pet-backend` restarted successfully; `https://pet.superstar.tots.asia/health` returns `{"status":"ok"}`; tokenless consult still returns expected `401 Missing token`; direct server-side FLU chat completion returned `{"ok":true}` with model `gpt-5.5`.
Fixed: email signup was blocked by Supabase `over_email_send_rate_limit` and the app was mistranslating auth errors as "invalid email." Added `POST /api/v1/auth/register` on the pet backend to create confirmed Supabase users with the service role, then updated the Flutter signup flow to call this endpoint and sign in immediately.
Verified: backend `npm run build` and `npm test` pass; Flutter `flutter test` passes; deployed and restarted `pet-backend`; live test `POST https://pet.superstar.tots.asia/api/v1/auth/register` returned 201 for a random test email, and Supabase password login then returned an access token.

Plan: rebuild AI consult from a single prompt/card generator into a production-style triage agent with explicit contract, state transitions, rule gating, Redis-backed state management with memory fallback, SSE events, and frontend rendering by response type.
Changed: added `backend/src/consult/` modules for contract, intent classification, emergency risk rules, Redis/memory state store, LLM triage gateway, agent orchestration, and SSE helpers. Refactored `POST /api/v1/consult` to use the new agent while preserving legacy response fields, and added `POST /api/v1/consult/stream`. Added `ioredis` dependency. Updated Flutter `ConsultScreen` to stop sending concatenated history as symptoms, keep a backend `session_id`, prefer the streaming endpoint, show state text while processing, and render guide/follow-up/emergency/triage responses differently. Updated `ApiService` with SSE parsing and fallback to the non-stream endpoint.
Verified: backend `npm run build` and `npm test` pass; Flutter `flutter test` passes. Deployed to `/opt/pet-backend`, ran server `npm install`, `npm run build`, and restarted `pet-backend`. Live checks: `/health` 200; tokenless consult 401; authenticated `hi` returns `response_type=guide`, `can_assess=false`, `risk_level=unknown`; authenticated “误食老鼠药” returns `response_type=emergency_alert`, `risk_level=emergency`, `seek_vet=true`; `/consult/stream` returns state and result SSE events; full symptom triage returns `response_type=triage_report`, `risk_level=medium`, provider `flu`, model `gpt-5.5`.
Next: add persistent conversation turn storage and a dedicated pet creation backend endpoint if client-side Supabase REST insert remains blocked by RLS; add deeper tests for nutrition/care intents and frontend SSE parser edge cases; configure `REDIS_URL` on production when Redis is provisioned.

Plan: continue hardening the rebuilt AI consult path by running stricter checks, fixing newly introduced frontend/backend edge bugs, expanding tests, and redeploying.
Fixed: replaced an invalid Flutter `CupertinoIcons.bolt_heart_fill` reference with a valid icon; removed a now-unused conversation-history helper; removed a redundant non-null assertion in the SSE client; made final-consult transcript generation include structured AI card content instead of empty assistant messages; included `session_id` when ending a consult; made `/api/v1/consult/stream` set SSE headers before authenticated invalid-input errors; changed consult-session persistence failures to warn without failing the user-facing consult response; stopped `backend/src/index.ts` from binding a real port when imported under tests.
Verified: backend `npm run build` passes; backend `npm test` now runs 10 tests, including greeting, vague symptom follow-up, emergency bypass, SSE state/result, invalid SSE input, and persistence-failure tolerance; Flutter `flutter test` passes; `flutter build ios --simulator --debug` succeeds. `flutter analyze` has no current compile errors from the consult work; it still exits non-zero due to pre-existing project-wide info-level lints/deprecations.
Deployed: synced backend to `/opt/pet-backend`, ran server `npm install`, `npm run build`, restarted `pet-backend`, and confirmed it is active. Live regression passed: `/health` 200; authenticated `hi` returns `guide`; `狗狗吐了` returns `follow_up`; `误食老鼠药` returns `emergency_alert`; full symptom triage returns `triage_report`, `medium`, provider `flu`, model `gpt-5.5`; authenticated invalid stream input returns text/event-stream error/done events; valid stream returns state/result events.

Plan: continue polishing AI consult toward a smoother production experience by removing brittle failure paths and improving interaction speed.
Changed: added a conservative rule-based triage fallback when all LLM providers fail or return invalid JSON, so sufficient symptom descriptions no longer collapse into a generic 503. Improved Redis fallback to stop retrying the failing primary store after switching to memory. Added SSE write protection for ended/destroyed responses. Added Flutter input focus management, clearer no-pet placeholder/welcome copy, and click-to-fill follow-up questions so the user can tap a follow-up card and immediately continue typing. Added backend regression coverage for provider outage fallback.
Verified: backend `npm run build` passes; backend `npm test` passes with 11 tests; Flutter `flutter test` passes; `flutter build ios --simulator --debug` succeeds. `flutter analyze` still exits non-zero because of pre-existing project-wide info-level lint/deprecation debt, with no new compile errors from this consult work.
Deployed: synced backend to `/opt/pet-backend`, ran server `npm install`, `npm run build`, restarted `pet-backend`, and confirmed it is active. Live regression passed again: `/health` 200; `hi` returns `guide`; `狗狗吐了` returns `follow_up`; `误食老鼠药` returns `emergency_alert`; full symptom triage returns `triage_report`, `medium`, provider `flu`, model `gpt-5.5`; valid stream returns state/result events.

Plan: finish the remaining project health cleanup so the app/backend are not carrying known analyzer or dependency-security failures.
Changed: applied Dart automatic fixes across Flutter `lib/`, migrated deprecated `Color.withOpacity(...)` calls to `withValues(alpha: ...)`, migrated Supabase initialization from deprecated `anonKey` to `publishableKey`, fixed the remaining async BuildContext analyzer issue, and fixed remaining flow-control lint. On the backend, ran `npm audit fix`, upgraded Vitest to `4.1.9`, and forced transitive `esbuild` to the fixed `0.28.1` via npm overrides.
Verified: Flutter `flutter analyze` now reports `No issues found`; `flutter test` passes; `flutter build ios --simulator --debug` succeeds. Backend `npm run build` passes; `npm test` passes with 11 tests on Vitest 4; `npm audit --json` reports zero vulnerabilities locally. Deployed backend to `/opt/pet-backend`; server `npm install` reports zero vulnerabilities; server `npm run build` and `npm test` pass; `pet-backend` restarted and is active. Live regression passed: `/health` 200; `hi` returns `guide`; `狗狗吐了` returns `follow_up`; `误食老鼠药` returns `emergency_alert`; full symptom triage returns `triage_report` from `flu/gpt-5.5`; valid stream returns state/result events.

Plan: complete the next production tasks after consult stabilization: add a backend pet creation path, enable production Redis for consult state, and add focused frontend card tests.
Changed: added `POST /api/v1/pets` backed by Supabase service role and authenticated by the user's bearer token; the endpoint ignores client-supplied `user_id` and always writes the token user id. Updated Flutter pet creation to call the backend endpoint for new pets while keeping existing pet edits on the current Supabase update path. Installed and enabled Redis on the VPS, added `REDIS_URL=redis://127.0.0.1:6379` to `/opt/pet-backend/.env`, and adjusted consult state storage so tests always use memory while production uses Redis without premature offline fallback. Made consult card widgets public enough for tests and added Flutter widget tests for guide/follow-up click, emergency card, and triage sections. Stabilized Vitest with a single-worker test script.
Verified: local backend `npm run build`, `npm test` (13 tests), and `npm audit --json` all pass with zero vulnerabilities. Local Flutter `flutter analyze` reports no issues, `flutter test` passes with 4 tests, and `flutter build ios --simulator --debug` succeeds. Server deploy: synced backend to `/opt/pet-backend`, server `npm install` shows zero vulnerabilities, server `npm run build` and `npm test` pass, `pet-backend` active, Redis active and `redis-cli ping` returns `PONG`. Live regression: register/login 201/200, `POST /api/v1/pets` returns 201 and writes the authenticated user id, `hi` returns guide, `狗狗吐了` returns follow_up, `误食老鼠药` returns emergency_alert, and SSE emits trace/state/result with `state_store=redis`.

## 2026-06-27

Plan: organize the large dirty worktree into safe, reviewable commits without committing local secrets, dependency folders, build outputs, nested repository metadata, generated caches, or large local media.
Changed: created grouped commits for project/deployment documentation, backend consult/auth/pets, Flutter auth/pet/consult UX, commerce storefront workspace, and small H5/hardware planning assets. Updated `.gitignore` to cover local agent settings, `.cursor`, Python caches, virtualenvs, and nested `.git` metadata.
Verified: each staged group passed `git diff --cached --check` before commit, except the uncommitted design reference package which was deliberately withdrawn because it contains many third-party/archived files with trailing whitespace and large binaries. Sensitive scans found no real API keys or passwords in committed diffs.
Skipped: `.claude/settings.local.json`, `.env`, `backend/.env`, `app/config.local.sh`, dependency/build/cache directories, `petsoul_h5/petsoul-bff/` nested repo, `petsoul_h5/设计参考/` reference package, and large hardware/design PDFs remain local and uncommitted.
Commits: `35223fb` docs/project state, `61811f2` backend production consult and pet APIs, `9ef9cba` Flutter app UX/API integration, `afe7f46` commerce storefront, `37d8262` H5 assets and hardware planning docs.

## 2026-07-07

Plan: restore the pet backend after the VPS appeared to have been wiped, preserving secrets where possible and verifying the production domain before touching app code.
Changed: recreated `/opt/pet-backend`, synced local `backend/` source and local `backend/.env`, restored production env keys for `NODE_ENV`, `PORT`, `REDIS_URL`, FLU base URL/model/API key, rebuilt the backend with Node 20, recreated and enabled `pet-backend.service`, recreated nginx config for `pet.superstar.tots.asia`, reinstalled certbot via snap, and issued a new Let's Encrypt certificate.
Verified: server-local `http://127.0.0.1:3000/health` returns 200; `https://pet.superstar.tots.asia/health` returns 200 from nginx HTTPS; HTTP redirects to HTTPS; tokenless `POST /api/v1/consult` returns `401 Missing token`; `pet-backend` and `nginx` are active/enabled; certificate expires on 2026-10-05.
Blocked: Redis could not be reinstalled yet because Ubuntu unattended-upgrade still holds the apt/dpkg lock. The backend runs with in-memory consult state fallback until Redis is reinstalled.
Found: the configured Supabase project `aktmdyxeqcmaldbylzfi.supabase.co` returns DNS NXDOMAIN from both server and local checks, and the old `srljyvqojhhwbgtdojkh.supabase.co` also returns NXDOMAIN. As a result, `/api/v1/auth/register` reaches the backend but returns `REGISTER_FAILED` with `fetch failed`; authenticated register/login/pets/consult regression is blocked until a valid Supabase URL and anon/service role keys are restored.
Cleanup: server-generated `/opt/pet-backend/package-lock.json` now includes the `esbuild@0.28.1` override needed for `npm ci`; pull it back to local once SSH/rsync is stable. Do not record server passwords or API keys in memory.
